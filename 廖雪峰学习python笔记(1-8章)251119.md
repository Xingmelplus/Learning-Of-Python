# 廖雪峰学习python笔记

### 1-3 略

判断对象的类型

```
>>>isinstance( lst , list )
>>>isinstance(  b  , tuple)
>>>isinstance(  c  , tuple)
>>>isinstance(  d  , Iterator)
true
true
```

### 4.1 使用文本编辑器

​	**Python的交互模式和直接运行`.py`文件有什么区别呢？**

​	直接输入`python`进入交互模式，相当于启动了Python解释器，但是等待你一行一行地输入源代码，每输入一	行就执行一行。

​	直接运行`.py`文件相当于启动了Python解释器，然后一次性把`.py`文件的源代码给执行了，你是没有机会以	交互的方式输入源代码的。

### 4.2 输入和输出

​	`print()`会依次打印每个字符串，遇到逗号`,`会输出一个空格，因此，输出的字符串是这样拼起来的：

![image-20251113192840034](C:\Users\Xingood\AppData\Roaming\Typora\typora-user-images\image-20251113192840034.png)

​	input() 语法：当你输入`name = input()`并按下回车后，Python交互式命令行就在等待你的输入了。这时，你可以输入任意字符，然后按回车后完成输入。

**注意**因为`input()`返回的数据类型是`str`，如果需要输入内容做判断时，由于`str`不能直接和整数比较，必须先把`str`转换成整数。Python提供了`int()`函数来完成这件事情：

```python
s = input('birth: ')
birth = int(s)              #转换
if birth < 2000:
    print('00前')
else:
    print('00后')
```

### 5.1 数据类型和变量

​	转义字符可以将  '和"打出

### 5.2 字符串和编码

​	**编码**

​	1个字节（byte）=8个比特（bit）

​	

​	Unicode，1个汉字=2个字节，1个字母=2个字节

​		Unicode是计算机内存编码，优点是统一、缺点是浪费存储

​	UTF-8中，1个汉字=3个字节，1个字母=1个字节

​	

​	文件存储用UTF-8，内存运算用Unicode

​	**格式化**

​	方法一  %

```Hello, %s' % 'world'
	>>> 'Hello, %s' % 'world'
	'Hello, world'
	>>> 'Hi, %s, you have $%d.' % ('Michael', 1000000)
	'Hi, Michael, you have $1000000.'
```

​	如果想打出%号，可以用%%

​	

​	方法二  f-string

	>>> r = 2.5
	>>> s = 3.14 * r ** 2
	>>>print(f'The area of a circle with radius {r} is {s:.2f}')
	The area of a circle with radius 2.5 is 19.62
​	以**f'**xxx {a}xxx{b}xxx{c}xxx**'**

### 5.3 使用list和tuple

列表：有序 可变

**追加元素**到末尾：

	>>> classmates.append('Adam')
	>>> classmates
	['Michael', 'Bob', 'Tracy', 'Adam']
**插入元素**到指定的位置（比如索引号为`1`的位置）

```>>> classmates.insert(1, 'Jack')
>>> classmates.insert(1, 'Jack')
>>> classmates
['Michael', 'Jack', 'Bob', 'Tracy', 'Adam']
```

**删除且弹出**末尾元素，用`pop()`方法：

```plain
>>> classmates.pop()
'Adam'
>>> classmates
['Michael', 'Jack', 'Bob', 'Tracy']
```

要**删除指定位置**的元素，用`pop(i)`方法，其中`i`是索引位置：

```plain
>>> classmates.pop(1)
'Jack'
>>> classmates
['Michael', 'Bob', 'Tracy']
```

要把某个元素**替换**成别的元素，可以直接赋值给对应的索引位置：

```plain
>>> classmates[1] = 'Sarah'
>>> classmates
['Michael', 'Sarah', 'Tracy']
```

#### tuple 元组



元组：有序 不变

```classmates = ('Michael', 'Bob', 'Tracy')```



定义一个元素的元组

```plain
t = (1,)
```



定义一个空元组

```t = (1,)```

定义一个“可变”的元组

```t=（'a','b',['c','d']）>>> t = ('a', 'b', ['A', 'B'])
>>> t = ('a', 'b', ['A', 'B'])
>>> t[2][0] = 'X'                   #更改元组中的列表
>>> t[2][1] = 'Y'
>>> t
('a', 'b', ['X', 'Y'])
```

@元组列表是一家，自己是一家

@列是方括号，园是圆括号

@列园有序

![tuple-1](https://liaoxuefeng.com/books/python/basic/list-tuple/step-1.png)



![tuple-2](https://liaoxuefeng.com/books/python/basic/list-tuple/step-2.png)

### 5.4 条件判断

**if 判值 :**

```python
age = 3
if age >= 18:
    print('adult')
elif age >= 6:
    print('teenager')
else:
    print('kid')
```



**判值可以简写为x**

```python
if x:
    dosomething
```

`x`是**非零**、**非空**为`True`，否则为`False`

### 5.5 模式匹配

**match**

```
score='B'

match score:
	case'A': DOSOMETHING
	case'B': dosomething
	case_:dosomething
```



```python
age = 15

match age:
    case x if x < 10:
        print(f'< 10 years old: {x}')
```

​	x会自动匹配age的值！很奇怪

##### 匹配列表

`match`语句还可以匹配列表，功能非常强大。

```python
args = ['gcc', 'hello.c', 'world.c']
# args = ['clean']
# args = ['gcc']

match args:
    # 如果仅出现gcc，报错:
    case ['gcc']:
        print('gcc: missing source file(s).')
    # 出现gcc，且至少指定了一个文件:
    case ['gcc', file1, *files]:
        print('gcc compile: ' + file1 + ', ' + ', '.join(files))
    # 仅出现clean:
    case ['clean']:
        print('clean')
    case _:
        print('invalid command.')
```



### 5.6 循环

​	两种循环 **for循环**和**while循环** 

​	

​	**for循环语法**

​	for 新 in 旧 ： dosomething

​	循环逻辑：**新**按顺序 使用**旧**列表的值 dosomething

​	**while循环语法**

​	while **值** ：dosomething

​	循环逻辑：dosomething中改变**值**



特殊指令：break

作用：中止循环

```python
n = 1
while n <= 100:
    if n > 10: # 当n = 11时，条件满足，执行break语句
        break # break语句会结束当前循环
    print(n)
    n = n + 1
print('END')
```

特殊指令： continue

作用：强启下一轮循环

```python
n = 0
while n < 10:
    n = n + 1
    if n % 2 == 0: # 如果n是偶数，执行continue语句
        continue # continue语句会直接继续下一轮循环，后续的print()语句不会执行
    print(n)
```

### 5.7 dict和set

#### dict

无序唯一

```>>> d = {'Michael': 95, 'Bob': 75, 'Tracy': 85} ```                                        

dict查找速度快，内存浪费多。

dict是空间换时间，内存换速度。

d [ ] 语法输出value

```plain
>>> d['Michael']
95
```

.get语法可以避免key不存在的情况；

.get语法会在key不存在时抛出预设值；

```plain
>>> d.get('Thomas')
>>> d.get('Thomas', -1)
-1
```

.pop语法可以删除key，对应的value也会从dict中删除

```plain
>>> d.pop('Bob')
75
>>> d
{'Michael': 95, 'Tracy': 85}
```



#### set

无序唯一

```plain
 s = {1, 2, 3}   #没有value 只含key的假字典 称为集合
```



列表**转为**集合

```plain
>>> s = set([1, 2, 3])
>>> s
{1, 2, 3}
```



添加和删除元素

```plain
>>> s.add(4)
>>> s
{1, 2, 3, 4}
>>> s.add(4)         #继续添加不会显示
>>> s
{1, 2, 3, 4} 


>>> s.remove(4)
>>> s
{1, 2, 3}
```



合并集合

```plain
>>> s1 = {1, 2, 3}
>>> s2 = {2, 3, 4}
>>> s1 & s2
{2, 3}
```

@字典集合是一家，可变唯一

@自己是一家

### 6.1 定义函数



定义函数

def  **f**（x）：dosomething and return something

```python
def my_abs(x):
    if x >= 0:
        return x
    else:
        return -x
```

@遇到`return`，中止函数，弹出结果。

没有```return```也可以



开头导入函数

```
from abstest import my_abs  # abstest需要在此.py文件同一目录下
```



空函数

```python
def nop():
    pass              # 无参数无内容
```



函数返回值 元组类型

```plain
>>> r = move(100, 100, 60, math.pi / 6)
>>> print(r)
(151.96152422706632, 70.0)
```

### 6.2函数参数

**多参数**

```python
def power(x, n):
    s = 1
    while n > 0:
        n = n - 1
        s = s * x
    return s
```



**默认参数**

```python
def power(x, n=2):
    s = 1
    while n > 0:
        n = n - 1
        s = s * x
    return s
```



**可变参数**

一星元组-可变参数

指个数可变

可以传入0到n个参数

作用一：生成**元组**，提供遍历

```python
def calc(*numbers):          #定义参数为可变参数
    sum = 0
    for n in numbers:
        sum = sum + n * n
    return sum
```

作用二：**解包赋值**普通参数

```plain
>>> def calc(a,b,c): dosomething
>>> nums = [1, 2, 3]
>>> calc(*nums)        #*nums表示解包这个list的所有元素，给普通参数赋值
14     

>>> def calc(a,b,c): dosomething
>>> n = (1, 2, 3)
>>> calc(*n)          #*nums表示解包这个tuple的所有元素，给普通参数赋值
14
```



**关键字参数**

两星字典-关键字参数

可以传入0到n个参数

关键字参数生成**字典**，字典名称为kw

```python
>>>def person(**kw):
    print(kw)
    
>>>person(name='zhaoxing',scores=100)   #Python 自动将参数名转换为 字符串 作为 字典键
{'name':'zhaoxing','scores':100}  
```

在关键字参数位置，可以传入字典，字典命名为extra

```plain
>>> extra = {'city': 'Beijing', 'job': 'Engineer'}

>>> person(name='zhaoxing',scores=100,**extra)                         #传入字典
{'name':'zhaoxing','scores':100,'city': 'Beijing','job': 'Engineer'}
```

**命名关键字参数：**

调用时必须传给关键字参数

一星元组，后接二星字典

```python
def person(name, age, *args, city, job):  #一星元组后
    print(name, age, args, city, job)
```

无一星元组，必须加一个`*`作为特殊分隔符

```python
def person(name, age, *, city, job):
    print(name, age, city, job)
```

**默认命名关键字参数**

一星后接默认二星字典

```python
def person(name, age, *, city='Beijing', job):#city='beijing'是命名关键字参数，并且带有默认值。
    print(name, age, city, job)
```

**参数组合**

参数定义的顺序必须是：必选参数、默认参数、可变参数、命名关键字参数和关键字参数

```def person
def person(   a,  =,  *,  =,  b  )
```

### 6.3递归函数

递归函数：函数在内部调用了这个函数本身

```
def fun(n):
	if n==1:
		return 1
	return n*fun(n-1)
```

## 7高级特性

### 7.1切片

only 列表、元组、字符串

```
list1=[]
list1[0:-1]  	#头-尾
list1[:]		#头-尾
list1[2:-2]		#第三-倒数第二个
list[-10:-5]	#取倒数第十-倒数第五

list1[:10:2]	#每隔2个取一个
[0, 2, 4, 6, 8]

tuple1=()
tuple(2:-2)		#元组

a='goodstudy'
a[2:-2]			#'odstud'

```

### 7.2 迭代

```
对象
├── 不可迭代对象：函数、类、模块
├── 可迭代对象  (Iterable) (实现了 __iter__())
|   |
│   ├── 普通可迭代对象 (只实现了 __iter__())
│   │   ├── 序列类型 (list, str, tuple, range)
│   │   ├── 字典 (dict)
│   │   ├── 集合 (set, frozenset)
│   │   └── 文件对象等
│   │
│   └── 迭代器对象(Iterator) 
│       ├── 迭代器对象 (实现了 __iter__() 和 __next__())
│       │   ├── 生成器 (generator)
│       │   ├── map, filter, zip 对象
│       │   ├── 枚举对象 (enumerate)
│       │   └── 自定义迭代器
```

省流版：

**可迭代对象**（Iterable）：实现了__iter__()方法

​	**普通**可迭代对象（Iterable）：只实现__iter__()方法

​		（包含基本数据类型list, str, tuple + 字典视图等）

​	**迭代器**对象（Iterator）：实现__iter__()和__next__()方法

​		（自己生成的，不占内存，惰性加载）



判断是否是可迭代对象：Iterable

```plain
>>> from collections.abc import Iterable
>>> isinstance('abc', Iterable) # str是否可迭代
ture
```



### 7.3 列表生成式

一种生成list的大法

```
a=[str(x)+"*"+str(x)+"="+str(x*x) for x in range(1,10)]
print(a)
```

for 后加 ***过滤条件***，注意不能有else

```
[str(x)+"*"+str(x)+"="+str(x*x) for x in range(1,10) if x>5 ]
```

for前加 ***表达式***，必须有else

```
[str(x)+"*"+str(x)+"="+str(x*x)  if x >5 x+=1 else -x for x in range(1,10) if x>5 ]
```

for **嵌套**

```
>>>print([ str(x)+str(y)  for x in [1,2,3] for y in ["q","w","e"]])
['1q', '1w', '1e', '2q', '2w', '2e', '3q', '3w', '3e']
```

for **两个变量**的列表生成式：遍历**d.items()** 

```
d={'a':5,'b':6,'c':7}
list1=[X1+str(X2) for X1,X2 in d.items()]				#i,o取字典d中的键值对
```

**d.items()** 

将**字典**转换成**字典视图**（一种普通可迭代对象），类似列表，列表包含元组，元组包含键和值

在列表生成式中，直接使用字典会得到键，拿不到键值对

```
>>>d = {'x': 'A', 'y': 'B', 'z': 'C'}
>>>print(d.items()) 
dict_items([('x', 'A'), ('y', 'B'), ('z', 'C')]) 	#可迭代对象：字典视图
```



### 7.4 生成器：generate

列表生成式--生成--list对象，耗内存

生成器--生成--迭代器对象，省内存

```plain
>>> L = [x * x for x in range(10)]
>>> L
[0, 1, 4, 9, 16, 25, 36, 49, 64, 81]			#生成列表,占用内存
>>> g = (x * x for x in range(10))
>>> g
<generator object <genexpr> at 0x1022ef630>		#生成可迭代对象，不占用内存
```

构建生成器法一：  **圆括号**+列表生成式

```
>>>g = (x * x for x in range(10))
>>>for i in g：
>>>	  print(i)
```

构建生成器法二：  **generate（）**

```
def fib(max):e
    n, a, b = 0, 0, 1
    while n < max:
        yield b				#生成可迭代对象
        a, b = b, a + b
        n = n + 1
    return 'done'
```

**generate（）**

```
a=fib(5)
next(a)			
1
next(a)
2				##调用对象，generate在yield处结束，等待下一次调用


next(fib(5))	
1
next(fib(5))
1				#直接调用，每次调用都会生成一个generate
```

常用for 调用 generate函数

```
for n in fib(6):
	print(n)
```

*但是用`for`循环调用generator时，发现拿不到generator的`return`语句的返回值。如果想要拿到返回值，必须捕获`StopIteration`错误，返回值包含在`StopIteration`的`value`中：*



## 8.1 高阶函数

函数可以赋给变量，变量可以指向函数（指向谁，就是等于谁）

高阶函数：函数可以作为变量，传入高阶函数中

#### map()

输入：一个函数、n个迭代对象 	

返回：一个非常规迭代对象

map(function, iterable , ...)

```plain
>>> list(map(str, [1, 2, 3, 4, 5, 6, 7, 8, 9]))  #一行代码实现转换list中每个元素
['1', '2', '3', '4', '5', '6', '7', '8', '9']
```

#### reduce()

输入：一个函数（输2返1），一个可迭代对象

返回：一个值

reduce(function, iterable , ...)

```python
reduce(f, [x1, x2, x3, x4]) = f(f(f(x1, x2), x3), x4)
```

### 8.2 filter

输入：一个函数（返回true or false），一个可迭代对象

返回：一个可迭代对象

`filter()`把传入的函数依次作用于每个元素，然后根据返回值是`True`还是`False`决定保留还是丢弃该元素。

### 8.3 sorted

排序序列

```plain
>>> sorted([36, 5, -12, 9, -21])
[-21, -12, 5, 9, 36]
```

`sorted()`可以接收一个`key`函数，实现自定义的排序

```plain
>>> sorted([36, 5, -12, 9, -21], key=abs)		#按绝对值大小排序
[5, 9, -12, -21, 36]				#key函数输入每一个元素，并返回一个元素，利用返回元素进行排序
```



