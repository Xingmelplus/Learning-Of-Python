# 此markdown用于记录C语言学习第二阶段（函数+数组+指针）

```c
/*定义函数*/
void sum (int begin, int end ){ dosomething;} ;
```

void：函数返回值的类型

sum：函数名称

（）：参数表

  {}   ：函数体



#### return

遇到return直接结束函数，并把后面东西交给调用函数的地方。



#### 函数原型

函数原型，把函数长什么样子告诉编译器。

```c
void sum(int a,int b){dosomething};			/*  声明 定义	*/
    sum(5,6);								/*  使用		 */

void sum(int a,int b) ;						/*  声明  */	 /*  即函数原型  */	
sum(5,6);							     	/*  使用  */
void sum(int a,int b){dosomething}; 		/*  定义  */
```

携程void sum（int，int）也可以。



#### 变量空间

每个函数有自己的变量空间，形式参数位于这个空间之中，和函数外没有关系。

大括号 { } 内的变量和 { } 外的变量泾渭分明，互不干扰。



## 数组

##### <类型>变量名称[元素数量]；

```c
int number[100] ;  /* 定义一个数组，该数组内包含int类型*/
number[0]=5;	   /* 向数组中赋值 */
number[1]=6;	   /* number是{5,6} */

int number[]={5,6} /* 初始化数组 */
int number[10]={0} /* 可以将number内所有数值 初始化为0 */
int number[10]={ [0]=2, [2]=3,6,}
				   /* 将number初始化，第一个为0，第三个为3，第四个为6 */

a=number[1]+2;	   /* 读取数组的值 */


```

##### 越界问题

[ ] 内是数组个数，且从0开始数

不允许越界赋值，和越界读取。

数组一旦创建，无法更改大小。

##### 数组大小

sizeof给出整个数组占据大小，单位是字节。

数组个数的表达方法：

sizeof（a）/ sizeof（ a[0] ）

常用于遍历中for（i=0，i<sizeof（a）/ sizeof（ a[0] ）,i++）{};

##### 二维数组

```c
int a[3][5]	;		/* 三行五列 */

int a[][5]={
    {0,1,2,3,4},
    {2,3,4,5,6},	/* n行五列,行数可以省略，列数不可省略 */
}
```

```c
a[0][0]	a[0][1]	a[0][2]	a[0][3]	a[0][4]
a[1][0]	a[1][1]	a[1][2]	a[1][3]	a[1][4]
a[2][0]	a[2][1]	a[2][2]	a[2][3]	a[2][4]
```

```
a[i][j]是一个int，表示第i行第j列上的元素；

遍历二维数组：
for(i=0; i<3;i++){
	for(j=0;j<5;j++){
		a[i][j]=i*j;
	}
}
```







