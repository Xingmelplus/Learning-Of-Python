## C语言学习（251125开始）

学习重点：（deepseek推荐）

1、基础语法：@变量、数据类型、@运算符

2、流程控制：@条件语句、@循环语句

3、函数：参数传递、返回值、作用域

4、指针：C语言精髓，重点掌握

5、数组和字符串

6、结构体和文件操作

# 浙江大学翁恺-C语言教程

# day1

##### 各种代码语言之间联系

机器语言（底层）：010100CF 001101AF		# 机器能懂

汇编语言（低级）：go run eat

代码语言（高级）：int a=6				       # 人编写

算法：定义一个整数型变量a值为6			    # 人能懂的流程意义图

##### 解释型语言和编译型语言

解释型：执行时，电脑**逐行解释**代码					python，又称脚本语言

编译型：执行前，电脑完整翻译代码，然后整块执行	     C语言、C++

##### 国际字符

非英文字符就是国际字符，就是需要用UTF-8编码

##### 注释 

/\*这是注释的内容\*/

//这是注释的内容

##### &&

**逻辑 “ 与 ” 是 &&**

不是 and

### 1 代码

##### printf(“hello world\n”)；

​	注意是      printf

​	注意结束用   ;

​	\n表示换行，即下一个printf打印内容换行	

##### %d

printf(“ “）中的 %d，表示%d将由后面的东西呈现

printf("%d",a)		   # a 变量

printf("%d",&a)		# a 地址（不常用）

scanf("%d",&b)		# 必须使用&，表示写入b地址，改变b变量，对应printf（"%d",b)

##### 口诀：scanf 带 &，printf 不带& 



##### int    x_01   =   500   ;

赋值，将右边的值赋给左边，进行初始化。

如果不初始化，会在内存中**读取默认值**



##### int   x , y , z  =6 ;	/* 同时赋值



##### scanf（）

出现在scanf中的东西是你一定要输入的东西，而不是给你看的东西

scanf( ”price %d %d“ , &a, &b) 中，一定要输入price 5 6（含5、6之间的空格）程序才会运行，

之后的&a，&b，是把读到的内容赋值到后面



##### 变量定义的位置要求

C99	     #允许在任何位置定义变量

ANSI C	#仅允许在代码开头处定义变量



##### const

const int x_01 = AMOUNT - price

修饰 int 的符号。表明 x_01 变量的值一旦初始化，就不能再修改了。



### 2 运算

##### 优先级

第一梯队：单目  + -

第二梯队：乘除  * / %

第三梯队：加减  + -

第四梯队：判断  ==  <  >  !=

第五梯队：赋值  =

##### 口诀：算帐、判了、戴帽子（赋值）



##### 赋值运算符=

赋值优先级最低u

赋值运算符也是一种公式，会返回值

例：a=6式子本身会返回6

例：a=b=6   ➡ a=(b=6)



##### % 

取余运算，舍商取余。

**/**

整数除法，向零取整，舍去小数。



##### 复合赋值

+=    -=     *=    /=   %=

复合赋值本身也会返回值，是a的值：整个表达式（a += 5）的值就是赋值后 a 的值



##### 递增运算符

++	单目运算符，一个算子。算子为变量，执行时变量+1

##### 递减运算符

--	同上

**注意**

a++和++a，都会执行a=a+1

区别在于返回值不同，a++是返回a+1之前的**a值**，++a返回a+1之后的**a+1值**

##### 口诀：a++和++a的返回值，谁在前，返回谁



### 3 判断

##### if

if  (a>b)  {dosomething ; }  else  {dosomething ; } ;

if  (a>b)  dosomething ; else dosomething;		#没有大括号，就把if后的第一句当作执行语句，不推荐！！

​											#else在前面没有大括号时，会和else之前的、最近的if匹配

```
if () {}  if () {}  if () {}				#合法，逐步执行，不会跳过

if () {} else if (){} else if(){}		#逐步执行，会跳过	
```



##### if-else if

if (a>b) { dosomething ; } else if (a>b-2) { dosomething ; };



##### switch-case

switch ( type ){ 

​	case  x_01: dosomething ; break;

​	case  x_02: dosomething ; break;

​	default : dosomething; }

type 和 x_01 、x_02 必须为 int类型

break 为结束的地方。如无break，当case匹配成功后，会继续执行后续case，直到break或结束。



##### 运算符优先级

优先级：算术运算符 > 关系运算符 > 赋值运算符

例如：5+6>2=b

##### 口诀：先算，后判，再赋



关系运算符

​	<  >  ==  !=

关系运算符优先级：

​	==   != 低于其他，最后判

​	平级运算符: 从左到右进行



### 4 循环

```
`while () {} ;				#和if一样
```

```
do { } while( );		#do循环体内至少执行一次，当while判否时结束
```

死循环：while循环体内没有改变循环条件的代码



##### rand()

生成一个随机数



##### rand()%100

通过取余算法得到一个**随机两位数**



### 5 for循环



```
for ( int i=1; i<=n ; i++) {dosomething};

//for 循环流程
1	int i=1
2	i<=n
3	{dosomething}		#注意，由判式直接进入循环体
4	i++
5	i<=n
6	{dosomething}		#注意，由判式直接进入循环体
...

```



##### break

多层for循环嵌套时，break只能破坏最内层一层的循环



##### goto

goto后面跟着一个label，用于定位和闪现

（仅仅建议在多重循环时使用）

```
for (){
	for(){
		goto newplace;		#goto 标签名
	}
}
newplace:					#标签名新定位
```



**return  continue  break  goto**

函数：return

循环switch：break、continue、goto



## 数据类型



##### sizeof()

运算符，返回 某个类型或变量在内存中占据的字节数

```
sizeof(char)=1		# 一个字节是8	bit
sizeof(short)=2
sizeof(int)=4		# 可以表示为寄存器的宽度
sizeof(long)=8
sizeof(long long)=8
```



##### 数据范围

计算机		现在默认使用 int

底层的时候	可以用char

```
char < short < int < float < double
```

```
char a=20	   			#char的意思是提供一个00000000
char a=255				#a=255  =>  11111111  =>  a=-1
printf a=-1			

unsigned char a=255		#unsigned的意思是 11111111	不等于  a=-1	不能表达负数了
printf a=255

```



##### 不同类型的范围

```
char a					#-128<a<127
int  b	   				#-32768<b<32767

float					# 32字长	7位有效数字		%f
double					# 64字长	15位有效数字		%lf
```



##### %e

科学计数法

适用于 float 和 double



##### 8进制和16进制

0开头：8进制

0x开头：16进制

```
printf("%o %x",a,b);		#8进制和16进制
```

#### 浮点数

( 默认使用 double，%lf )

##### %.5f	 五位小数

##### %.8lf	八位小数

百分号和 f 之间表示小数点后几位



##### 123f

```
double a=3.14;	√		#3.14默认是double

float b=3.14;	×		#报错，3.14默认double，赋值给float的话精度不够
float c=3.14f;	√
```



##### 判断浮点数相等

求差判断是否小于某一个值！



##### int  float  double

整数：int

小数（浮点数）：float、double

​	double：高精度，耗内存。（推荐）（目前使用小数时默认double）

​	float：低精度，省内存，适用嵌入式（默认不推荐）

**<u>口诀：就用int或double，一个%d，一个% lf</u>**



```
int nums_01 = 123;
float nums_02=3.14f;		#即使已经在前面声明了float，也要在数字后面加f
double nums_03=3.1415926;

printf("%d\n", nums_01);  	#%d   int	 (默认整数格式)
printf("%f\n", nums_02);		#%f   float
printf("%lf\n",nums_03);	#%lf  double (默认小数格式)
```

##### 



#### 字符类型—char

char类型变量在赋值时，得到整数时，就保存整数，得到字符时，就先转整数 再保存

注意：

```
char c1 = 'a';   // ✅ 正确：单个文本字符，c1以数字储存
char c2 = '1';   // ✅ 正确：单个文本数字，c2以数字储存：49
char ch = 12;    // ✅ 正确：数值数字,-127到128之间。ch以数字12储存，输出时也可以转化成字符。

char ch = "ab";   // ⚠️ 错误！类型不匹配
char ch = '12';   // ⚠️ 编译器允许，但有警告
```

##### 输出

char类型变量的输出可以用 %c、也可以用%d

%c	将储存的整数 转化为ASCII字符

%d	输出储存的整数

```
char a=1;
字符//  功能符号....     '0'、'1'、'2'、'3'、'4'..     'A'、'B'、'C'、'D'....
整数// 1、2、3、4....    48、 49、 50、 51、 52...     65、 66、 67、 68....
```

##### 输入

```
scanf("%d",&a);			#输入整数，仅能输入整数！
scanf("%c",&b);			#输入字符，'1'、'2'、'3'、'A'、'B'、'C'、'@'、'+'、'&'
```

**口诀：输入%c 文本、输入%d整数**



#### 逃逸字符

\b

\\"



**自动类型转换**

```c
char> short> int> long> long long

int> float> double
```

 scanf 中 ，float是float、double是double

printf中 float、double自动转化为double



**强制类型转变**

会返回一个括号内的类型的值（），如下：

```c
int i =（int）200.5；		/*printf i>>>200
```



**bool类型**

```
#include<stdio.h>   /*必须先引入

bool b = 6>5;		/*其实bool也是0或1
```

####  逻辑运算

```c
!  非
&& 与
|| 或

dosomething1 && dosomething2		/* 如果dosomething1为否，则dosomething2也不会执行，称短路！
```

优先级

​	赋值优先级最低，逻辑第二低



嵌套条件表达式

```
int a = b>2 ? 5 : 8			/* b>2为真时，返回5，否则返回8
```

​	嵌套条件表达式的运算规则 **从右到左**

```
int max = (a > b) ? a : (b > c) ? b : c;		/*先执行右边，再执行左边
int max = (a > b) ? a : ((b > c) ? b : c);
```



